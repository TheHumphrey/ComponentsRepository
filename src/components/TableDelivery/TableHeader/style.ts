import styled from 'styled-components';

const TableHeaderStyle = styled.tr`
  span {
    font-size: 12px;
  }

  .temp {
    color: #0000ff;
  }

  .money {
    color: #00d27a;
  }
`;

export default TableHeaderStyle;
